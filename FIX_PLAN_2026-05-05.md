# ACDA-Q 修复规划报告

**日期**: 2026-05-05
**修复范围**: 回测权限问题 + 策略下拉框 + pre_close 数据链路 + getattr 沙箱绕过

---

## 一、问题清单与根因

| # | 问题 | 根因 | 当前代码状态 |
|---|------|------|-------------|
| 1 | Permission denied: `/app/reports/` | Celery worker 容器以 `quant` 用户(uid=999)运行，bind mount 的 `reports` 目录属主为宿主机 `ubuntu`(uid=1000)，权限不匹配 | **待修复**（docker-compose.yml 已加 `user` directive，需重建容器生效） |
| 2 | 策略回测下拉框为空 | StrategyEditorPage 把策略存在本地 Tauri Store，StrategyBacktestPage 却调服务器 API `/strategies` 拉取 | **已修改**（StrategyBacktestPage.tsx 改为从本地 Store 读取） |
| 3 | pre_close 缺失导致涨跌停检查失效 | 评审报告 P1-A 记录的问题 | **已修复**（数据库表有 `pre_close` 字段，timescale.py SQL 已包含，datafeed.py mock 数据已生成，broker.py 已读取） |
| 4 | getattr 绕过沙箱 | 评审报告 P1-B 记录的问题 | **已修复**（executor.py `BLACKLISTED_NAMES` 已包含 `getattr/hasattr/setattr`，restricted builtins 已排除） |

---

## 二、修复步骤

### 步骤 1：确认前端修改生效

StrategyBacktestPage.tsx 已改为从 `useStrategyStore`（本地 Tauri Store）读取策略列表，提交回测时带上 `strategy_code`。需要确认前端已重新编译/热重载。

### 步骤 2：验证 pre_close 数据链路

- [x] 数据库 migration `002_market_data.sql` 中 `daily_bars` 表已定义 `pre_close DECIMAL(12, 4)`
- [x] `timescale.py::query_daily_bars()` SELECT 列表已包含 `pre_close`
- [x] `timescale.py::get_adj_price()` SELECT 列表已包含 `d.pre_close`
- [x] `datafeed.py::_generate_mock_bars()` 已生成 `pre_close = np.roll(close, 1)`
- [x] `broker.py::execute_orders()` 第 145 行已读取 `bar['pre_close']`

### 步骤 3：验证 getattr 沙箱拦截

- [x] `executor.py::BLACKLISTED_NAMES` 已包含 `"getattr", "hasattr", "setattr", "delattr"`
- [x] AST Call 检查会拦截 `getattr(...)` 直接调用
- [x] `_create_restricted_globals()` 已将 `getattr` 排除在 restricted builtins 之外

### 步骤 4：重建容器修复权限（方案 B）

```bash
cd ~/ACDA-Q
docker compose down
docker compose up -d --build
```

docker-compose.yml 中已为 `api` 和 `worker` 服务添加：
```yaml
user: "${UID:-1000}:${GID:-1000}"
```

重建后容器以宿主机用户运行，bind mount 的 `reports` 目录读写权限自然对齐。

---

## 三、验证清单

修复完成后，按以下顺序验证：

1. **策略下拉框**: 打开"策略回测"页面，下拉框应显示"策略中心"中创建的策略
2. **pre_close 链路**: 跑双均线回测，查看日志确认无 `pre_close` 相关警告
3. **涨跌停生效**: 构造极端行情数据测试涨停买入/跌停卖出是否被正确拦截
4. **沙箱拦截**: 尝试提交含 `getattr(obj, "__class__")` 的策略，应返回安全错误
5. **报告写入**: 回测完成后，`~/ACDA-Q/reports/` 目录下应生成对应 UUID 的 JSON 文件

---

## 四、风险评估

- **容器重建**会导致 API 和 Worker 短暂中断（约 1-2 分钟）
- **前端修改**为纯逻辑改动，不影响数据格式或 API 契约
- **pre_close/getattr** 为代码已修复项，本次仅需验证，无引入新 bug 风险
